<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

    <% if @movie.filename? %>

        <%= f.text_field :filename, :readonly => true %>
        <%= link_to 'Delete attachment', :action => :delete_attachment, :id => @movie.id %>
     <% else %>

  <div class="field">
    <%= f.label :Fichier %><br>
    <%= f.file_field :file %>
  </div>
  
  <div class="actions">
<% end %>

    <%= f.submit %>
  </div>
<% end %>
